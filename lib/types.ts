export type Atom = {
    createdAt: string
    name: string
    id: string
  }
  
  export type Product = {
    createdAt: string
    title: string
    isCompleted: boolean
    id: string
    productId: string
  }